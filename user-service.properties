# Service name for Eureka
spring.application.name=user-service

# Eureka Discovery
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# MySQL Database Configuration
spring.datasource.url = jdbc:mysql://localhost:3306/Banking_Users
spring.datasource.username = root
spring.datasource.password = Fabulous1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

# Eureka Client
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# Config Server (if used)
spring.cloud.config.uri=http://localhost:8888/user-service/default

# Server Port
server.port=8081

